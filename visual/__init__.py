"""Clage visualization layer: pure consumer of recorded simulation/experiment data.

This package imports only the standard library and matplotlib. It contains no
evolutionary logic — it never runs `neat`, `world`, or `experiments` code. Use
`python -m visual ...` (see `visual.app`).
"""
